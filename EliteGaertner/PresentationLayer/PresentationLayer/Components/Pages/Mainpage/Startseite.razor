@page "/Startseite"
@using Microsoft.AspNetCore.Components.Web
@inject NavigationManager Nav
@rendermode InteractiveServer

<div class="page-container">
    
    <div class="header-bar">
        <div class="rangliste">
            <div class="rangliste-icon">
                <img src="pictures/Rangliste.png" alt="Rangliste" height="40" /> 
                <span style="font-size: 24px;">Rangliste</span>
                <div class="bar-chart"></div>
            </div>
            <p>Rangliste</p>
        </div>
        <div class="menu-icon-container">
            <span class="menu-icon" @onclick="ToggleLogoutMenu">&#x2261;</span>
        </div>
    </div>

    @if (showLogout)
    {
        <div class="logout-menu">
            <p @onclick="NavigateToLogout">Logout &lt;</p>
        </div>
    }

    <div class="tinder-card-area">
        @if (isProfileVisible)
        {
            <div class="tinder-card">
                <div class="card-image-placeholder">
                    <img src="pictures/Testbilder/Gurken_01.jpg" alt="Erntebild" class="harvest-image" />
                </div>

                <div class="info-box">
                    <div class="details">
                        <p><strong>Gurken</strong></p>
                        <p>Breite - cm</p>
                        <p>Länge - cm</p>
                        <p>Gewicht - kg</p>
                    </div>
                    <div class="comment">
                        Hier könnte ein Kommentar zur Ernte stehen.
                        <p class="username">@user_name1</p>
                    </div>
                </div>

                <div class="rating-icons">
                    <div class="rating-icon flag-icon" @onclick="FlagProfile">
                        <span style="font-size: 20px;">🚩</span>
                        <div class="notification-dot">1</div>
                    </div>
                    <div class="rating-icon dislike-icon" @onclick="DislikeProfile">
                        <span style="font-size: 24px;">❌</span>
                    </div>
                    <div class="rating-icon like-icon" @onclick="LikeProfile">
                        <span style="font-size: 24px;">💚</span>
                    </div>
                    <div class="rating-icon settings-icon" @onclick="NavigateToPreferences">
                        <span style="font-size: 20px;">⚙️</span>
                    </div>
                </div>

            </div>
        }
        else
        {
            <p style="font-size: 18px; font-weight: bold; text-align: center;">
                Profil wurde gemeldet und ausgeblendet.
            </p>
        }
        
        @if (showReportModal)
        {
            <div class="report-modal-overlay">
                <div class="report-modal">
                    
                    <p style="font-weight: bold; margin-bottom: 10px;">Post Melden</p>
                    
                    <div class="report-reason">
                        <input type="checkbox" id="abstossende" @onchange="@(e => UpdateReportReason("abstossende Inhalte", e))" checked="@(selectedReportReason == "abstossende Inhalte")" />
                        <label for="abstossende">abstossende Inhalte</label>
                    </div>
                    
                    <div class="report-reason">
                        <input type="checkbox" id="sexuelle" @onchange="@(e => UpdateReportReason("sexuelle Inhalte", e))" checked="@(selectedReportReason == "sexuelle Inhalte")" />
                        <label for="sexuelle">sexuelle Inhalte</label>
                    </div>
                    
                    <div class="report-reason">
                        <input type="checkbox" id="gewaltvolle" @onchange="@(e => UpdateReportReason("gewaltvolle Inhalte", e))" checked="@(selectedReportReason == "gewaltvolle Inhalte")" />
                        <label for="gewaltvolle">gewaltvolle Inhalte</label>
                    </div>
                    
                    <div class="report-reason">
                        <input type="checkbox" id="catfishing" @onchange="@(e => UpdateReportReason("Catfishing", e))" checked="@(selectedReportReason == "Catfishing")" />
                        <label for="catfishing">Catfishing</label>
                    </div>
                    
                    <div class="report-reason">
                        <input type="checkbox" id="spam" @onchange="@(e => UpdateReportReason("Spam", e))" checked="@(selectedReportReason == "Spam")" />
                        <label for="spam">Spam</label>
                    </div>

                    <div class="modal-actions">
                        <button class="report-button" @onclick="ReportProfile">Melden</button>
                        <div class="close-modal-icon" @onclick="CloseReportModal">
                            <span style="font-size: 20px;">❌</span>
                        </div>
                    </div>
                </div>
            </div>
        }

    </div>

    <footer class="bottom-nav">
        <div class="nav-item active" @onclick="GoToMatches">
            <img src="pictures/MatchesIcon.png" alt="Matches" height="40" /> 
            <span>Matches</span>
        </div>
        
        <div class="nav-item" @onclick="GoToMainpage">
            <img src="pictures/EliteGärtner_logo.png" alt="EliteGärtner" height="40" /> 
            <span class="elitegaertner-title mt-2" style="font-weight: 600; font-size: 1.0rem">EliteGärtner.</span>
        </div>
        
        <div class="nav-item" @onclick="GoToProfile">
            <img src="pictures/Profil_04.png" alt="Profile" height="40" /> 
            <span>Profil</span>
        </div>
    </footer>
</div>

@code {
    private bool showLogout = false;
    private bool showReportModal = false; // NEU: Steuert die Sichtbarkeit der Meldemaske
    private bool isProfileVisible = true; // NEU: Steuert die Sichtbarkeit der Karte (Profil)
    private string selectedReportReason = ""; 
    private string user_name1 = "@user_name1";

    
    private void ToggleLogoutMenu()
    {
        showLogout = !showLogout;
    }

    private void NavigateToLogout()
    {
        // Logik für Logout
    }

    private void NavigateToPreferences()
    {
        Nav.NavigateTo("/praeferenzeinstellungen", forceLoad: true);
    }

    private void GoToMatches()
    {
        Nav.NavigateTo("/MatchesTest", forceLoad: true);
    }

    private void GoToMainpage()
    {
        Nav.NavigateTo("/startseite", forceLoad: true);
    }

    private void GoToProfile()
    {
        Nav.NavigateTo("/ProfilTest", forceLoad: true);
    }

    // NEU: Methode zum Anzeigen der Meldemaske
    private void FlagProfile()
    {
        showReportModal = true;
        Console.WriteLine("Meldemaske geöffnet");
    }

    // NEU: Methode zum Schließen der Meldemaske
    private void CloseReportModal()
    {
        showReportModal = false;
        Console.WriteLine("Meldemaske geschlossen");
    }

    // NEU: Methode zur Aktualisierung des ausgewählten Meldegrunds
    private void UpdateReportReason(string reason, ChangeEventArgs e)
    {
        if ((bool)e.Value)
        {
            // Setzt den ausgewählten Grund und deaktiviert alle anderen
            selectedReportReason = reason;
        }
        else if (selectedReportReason == reason)
        {
            // Wenn der aktuell ausgewählte Grund abgewählt wird, wähle keinen aus
            selectedReportReason = string.Empty;
        }
    }

    // NEU: Methode zum Melden des Profils (Use-Case 2.7)
    private void ReportProfile()
    {
        if (!string.IsNullOrEmpty(selectedReportReason))
        {
            Console.WriteLine($"Profil gemeldet. Grund: {selectedReportReason}");
            // Logik: Profil wird dem User nicht mehr angezeigt.
            isProfileVisible = false; // Das Profil ausblenden
            showReportModal = false;  // Maske schließen

            // Hier müsste die Logik für das Laden des nächsten Profils folgen.
        }
        else
        {
            Console.WriteLine("Bitte wählen Sie einen Meldegrund.");
        }
    }

    // Dummy-Methoden für Bewertungen
    private void DislikeProfile() => Console.WriteLine("Dislike");
    private void LikeProfile() => Console.WriteLine("Like");
    
}



<style>
    /* Definiere die Hauptfarben */
    :root {
        /* Ähnlich dem Lilaton der Aubergine */
        --main-purple: #8B45B3;
        --dislike-red: #D62828;
        --like-green: #4CAF50;
        --border-color: #000;
    }

    /* Allgemeine Layout-Stile */
    .page-container {
        display: flex;
        flex-direction: column;
        height: 100vh;
        background-color: #f7f7f7; /* Leichter Hintergrund */
        position: relative;
        font-family: Arial, sans-serif; /* Basis-Schriftart */
        overflow: hidden;
    }

    /* --- Header-Bar (Rangliste und Menü) --- */
    .header-bar {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 10px 15px;
        border-bottom: 1px solid #ccc;
        background-color: #fff;
        z-index: 10;
    }

    .rangliste {
        display: flex;
        align-items: center;
        font-weight: bold;
        font-size: 14px;
    }

    .rangliste-icon {
        margin-right: 5px;
        /* Dies ist ein Platzhalter für das eigentliche Icon-Design */
    }

    .menu-icon-container {
        cursor: pointer;
    }

    .menu-icon {
        font-size: 30px;
        font-weight: bold;
        line-height: 1;
        color: var(--border-color);
    }

    /* --- Logout-Menü (Dropdown) --- */
    .logout-menu {
        position: absolute;
        top: 50px; /* An die Header-Bar anpassen */
        right: 15px;
        width: 150px;
        background-color: #fff;
        border: 1px solid var(--border-color);
        border-radius: 5px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        z-index: 20;
        padding: 5px 0;
    }

    .logout-menu p {
        padding: 5px 10px;
        margin: 0;
        cursor: pointer;
        font-size: 16px;
        text-align: right;
    }

    .logout-menu p:hover {
        background-color: #eee;
    }

    /* --- Haupt-Karten-Bereich --- */
    .tinder-card-area {
        flex-grow: 1;
        display: flex;
        justify-content: center;
        align-items: center;
        padding: 20px;
        position: relative;
    }

    .tinder-card {
        width: 100%;
        max-width: 400px; /* Maximale Breite für das "Smartphone"-Layout */
        height: 80vh; /* Relative Höhe zur Ansichtshöhe */
        max-height: 600px;
        border: 3px solid var(--border-color);
        border-radius: 10px;
        background-color: #fff;
        box-shadow: 5px 5px 0 0 var(--border-color); /* Sketch-Effekt */
        position: relative;
        overflow: hidden;
        display: flex;
        flex-direction: column;
    }

    .card-image-placeholder {
        width: 100%;
        /* Berechnet die Höhe, damit der untere Bereich Platz für Icons und Infos hat */
        flex-grow: 1;
        background-color: #eee;
        overflow: hidden;
    }

    .harvest-image {
        width: 100%;
        height: 100%;
        object-fit: cover; /* Stellt sicher, dass das Bild den Container ausfüllt */
    }

    /* --- Info-Box (Gurken-Details) --- */
    .info-box {
        position: absolute;
        bottom: 20px; /* Über den Rating-Icons */
        left: 20px;
        right: 20px;
        background-color: rgb(176, 66, 255); /* Transparente braune/rötliche Farbe */
        color: white;
        padding: 10px;
        border-radius: 8px;
        border: 1px solid var(--border-color);
        box-shadow: 2px 2px 0 0 var(--border-color);
        display: flex;
        font-size: 14px;
    }

    .details {
        flex: 1;
        padding-right: 10px;
        border-right: 1px solid white;
    }

    .comment {
        flex: 2;
        padding-left: 10px;
    }

    .username {
        font-style: italic;
        font-weight: bold;
        margin-top: 5px;
    }

    /* --- Bewertungs-Icons (Mitte-Unten) --- */
    .rating-icons {
        position: absolute;
        bottom: -25px;
        left: 50%;
        transform: translateX(-50%);
        display: flex;
        gap: 15px;
        background-color: #fff;
        padding: 10px;
        border-radius: 50px;
        z-index: 5;
    }

    .rating-icon {
        width: 40px;
        height: 40px;
        border-radius: 50%;
        display: flex;
        justify-content: center;
        align-items: center;
        cursor: pointer;
        border: 2px solid var(--border-color);
        position: relative;
        box-shadow: 2px 2px 0 0 var(--border-color);
        transition: transform 0.1s ease;
    }

    .rating-icon:active {
        transform: translate(-50%, 0) scale(0.95);
        box-shadow: 1px 1px 0 0 var(--border-color);
    }

    .dislike-icon {
        background-color: var(--dislike-red);
    }

    .like-icon {
        background-color: var(--like-green);
    }

    .flag-icon, .settings-icon {
        background-color: #fff;
    }

    .notification-dot {
        position: absolute;
        top: -5px;
        right: -5px;
        background-color: var(--dislike-red);
        color: white;
        font-size: 10px;
        font-weight: bold;
        border-radius: 50%;
        width: 15px;
        height: 15px;
        display: flex;
        justify-content: center;
        align-items: center;
        border: 1px solid var(--border-color);
    }

    /* --- Bottom Nav (Unten) --- */
    .bottom-nav {
        display: flex;
        justify-content: space-around;
        padding: 10px 0;
        border-top: 3px solid var(--border-color);
        background-color: #fff;
        z-index: 10;
    }

    .nav-item {
        display: flex;
        flex-direction: column;
        align-items: center;
        cursor: pointer;
        font-size: 12px;
        font-weight: bold;
    }

    .nav-icon-wrapper {
        position: relative;
    }

    .nav-icon {
        width: 50px; /* Größe der Icons anpassen */
        height: 50px;
        /* Optional: Icons wie in der Vorlage darstellen (Sketch-Look) */
        border-radius: 50%;
        border: 2px solid var(--border-color);
        box-shadow: 2px 2px 0 0 var(--border-color);
    }

    /* Spezieller Stil für EliteGärtner (Aubergine) */
    .nav-item:nth-child(2) .nav-icon {
        background-color: var(--main-purple);
        /* Hier ist das Bild der Aubergine (elitegaertner_icon.png) */
    }

    /* Spezieller Stil für Matches (Gurkenhände) */
    .nav-item:nth-child(1) .nav-icon {
        /* Hier ist das Bild der Gurkenhände (matches_icon.png) */
        background-color: #fff;
    }

    /* Spezieller Stil für Profil (Gärtner) */
    .nav-item:nth-child(3) .nav-icon {
        /* Hier ist das Bild des Gärtners (profil_icon.png) */
        background-color: #fff;
    }

    .small-dot {
        top: 0;
        right: 0;
        width: 10px;
        height: 10px;
        font-size: 7px;
    }

    /* --- Modal für unangemessene Inhalte melden --- */
    .report-modal-overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        display: flex;
        justify-content: center;
        align-items: center;
        background-color: rgba(0, 0, 0, 0.1); /* Leicht transparentes Overlay */
        z-index: 50; /* Über allen anderen Elementen */
    }

    .report-modal {
        width: 80%;
        max-width: 300px;
        background-color: #fff;
        padding: 20px;
        border: 3px solid var(--border-color);
        border-radius: 10px;
        box-shadow: 5px 5px 0 0 var(--border-color); /* Sketch-Effekt */
        position: relative;
    }

    .report-reason {
        display: flex;
        align-items: center;
        margin-bottom: 8px;
        font-size: 14px;
    }

    .report-reason input[type="checkbox"] {
        margin-right: 10px;
        width: 15px;
        height: 15px;
    }

    .modal-actions {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-top: 15px;
    }

    .report-button {
        background-color: var(--dislike-red);
        color: white;
        border: 2px solid var(--border-color);
        border-radius: 5px;
        padding: 8px 15px;
        font-weight: bold;
        cursor: pointer;
        box-shadow: 2px 2px 0 0 var(--border-color);
        transition: transform 0.1s ease;
    }

    .report-button:active {
        transform: translate(1px, 1px);
        box-shadow: 1px 1px 0 0 var(--border-color);
    }
    
    .close-modal-icon {
        background-color: #fff;
        border: 2px solid var(--border-color);
        border-radius: 50%;
        width: 30px;
        height: 30px;
        display: flex;
        justify-content: center;
        align-items: center;
        cursor: pointer;
        box-shadow: 2px 2px 0 0 var(--border-color);
    }
</style>