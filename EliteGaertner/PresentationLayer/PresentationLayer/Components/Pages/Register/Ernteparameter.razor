@page "/ernte-hochladen"
@rendermode InteractiveServer
@inject NavigationManager Nav

<link href="https://fonts.googleapis.com/css2?family=Patrick+Hand&display=swap" rel="stylesheet">
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600&display=swap" rel="stylesheet">

<div class="register-page-container">
    <div class="login-center">

        <div class="register-card ernte-parameter-card">

            <div class="logo-container">
                <img src="pictures/EliteGärtner_logo.png" alt="EliteGärtner Logo" class="app-logo" />
                <h2 class="elitegaertner-title">EliteGärtner.</h2>
            </div>

            <h3 class="register-title">Deine Ernte veröffentlichen</h3>
            
            <div class="form-layout">

                <div class="left-column">
                    <div class="ernte-image-container">
                        @if (HochgeladenesBild == null)
                        {
                            <div class="image-placeholder">
                                <span>Lade ein Bild deiner Ernte hoch</span>
                                <span class="bi bi-camera" style="font-size: 3rem;"></span>
                            </div>
                        }
                        else
                        {
                            <img src="@HochgeladenesBild" alt="Erntebild" class="ernte-image" />
                        }
                    </div>

                    <InputFile id="file-upload" OnChange="HandleFileSelection" hidden />
                    <label for="file-upload" class="app-button secondary-button upload-button">
                        <span class="bi bi-upload"></span> Bild hochladen
                    </label>

                </div>

                <div class="right-column">
                    <h4 class="tag-headline">Gemüse</h4>
                    <div class="tag-grid">
                        @foreach (var tag in GemueseTags)
                        {
                            <div class="tag app-tag @(SelectedTag == tag.Name ? "selected" : "")"
                                 @onclick="() => ToggleTag(tag.Name)">
                                <span>@tag.Emoji</span> <span>@tag.Name</span>
                            </div>
                        }
                    </div>

                    <h4 class="tag-headline">Obst</h4>
                    <div class="tag-grid">
                        @foreach (var tag in ObstTags)
                        {
                            <div class="tag app-tag @(SelectedTag == tag.Name ? "selected" : "")"
                                 @onclick="() => ToggleTag(tag.Name)">
                                <span>@tag.Emoji</span> <span>@tag.Name</span>
                            </div>
                        }
                    </div>

                    <div class="parameter-row">
                        <div class="input-group">
                            <label class="input-label">Gewicht (in g)</label>
                            <input type="number" class="app-input" @bind="Gewicht" placeholder="z.B. 300" />
                        </div>

                        <div class="input-group">
                            <label class="input-label">Größe (in cm)</label>
                            <input type="number" class="app-input" @bind="Groesse" placeholder="z.B. 25" />
                        </div>
                    </div>
                    
                    <h4 class="tag-headline">Kommentar</h4>
                    <div class="input-group">
                        <textarea class="app-input" maxlength="150" @bind="Kommentar"
                                      placeholder="Beschreibe kurz deine Ernte..."></textarea>
                        <div class="char-counter">@Kommentar.Length / 150</div>
                    </div>
                </div>
            </div>

            <button class="app-button primary-button weiter-button"
                    disabled="@( string.IsNullOrEmpty(SelectedTag) || string.IsNullOrEmpty(HochgeladenesBild) )"
                    @onclick="Veröffentlichen">
                Ernte veröffentlichen
            </button>
            
            @if (string.IsNullOrEmpty(SelectedTag) || string.IsNullOrEmpty(HochgeladenesBild))
            {
                <p class="disabled-hint">Bitte lade zuerst ein Bild hoch und wähle ein Tag aus.</p>
            }
        </div>
    </div>
</div>

@code {
    private string? HochgeladenesBild = null;

    public class TagItem(string name, string emoji)
    {
        public string Name { get; set; } = name;
        public string Emoji { get; set; } = emoji;
    }
    
    List<TagItem> GemueseTags = new()
    {
        new("Auberginen", "🍆"), new("Gurken", "🥒"), new("Tomaten", "🍅"), new("Kürbisse", "🎃"), 
        new("Paprika", "🌶️"), new("Zucchini", "🥒"), new("Kartoffeln", "🥔"), new("Karotten", "🥕"), 
        new("Salate", "🥬"), new("Zwiebeln", "🧅")
    };
    List<TagItem> ObstTags = new()
    {
        new("Melonen", "🍉"), new("Äpfel", "🍎"), new("Birnen", "🍐"), new("Pfirsiche", "🍑"), 
        new("Kirschen", "🍒"), new("Erdbeeren", "🍓"), new("Trauben", "🍇")
    };

    string? SelectedTag = null; 
    int? Groesse { get; set; }
    int? Gewicht { get; set; }
    string Kommentar { get; set; } = "";
    
    void ToggleTag(string tag)
    {
        SelectedTag = (SelectedTag == tag) ? null : tag;
    }

    private async Task HandleFileSelection(InputFileChangeEventArgs e)
    {
        var file = e.File;
        if (file != null)
        {
            try
            {
                var maxFileSize = 5 * 1024 * 1024;
                if (file.Size > maxFileSize)
                {
                    Console.WriteLine("Die Datei ist zu groß.");
                    return;
                }
                
                var stream = file.OpenReadStream(maxFileSize);
                
                using var memoryStream = new MemoryStream();
                await stream.CopyToAsync(memoryStream);
                var base64String = Convert.ToBase64String(memoryStream.ToArray());
                
                HochgeladenesBild = $"data:{file.ContentType};base64,{base64String}";
                
            }
            catch (Exception ex)
            {
                Console.WriteLine($"Fehler beim Hochladen der Datei: {ex.Message}");
            }
        }
    }

    void Veröffentlichen()
    {
        if (string.IsNullOrEmpty(SelectedTag) || string.IsNullOrEmpty(HochgeladenesBild)) return;

        Console.WriteLine($"Veröffentlicht: {SelectedTag}, Bildgröße: {HochgeladenesBild.Length}");

        Nav.NavigateTo("/ProfilTest", forceLoad: true);
    }
}


<style>
    
    .register-page-container {
        background-image: url('pictures/LightIconBackground.jpeg');
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;
        height: 100%;
        min-height: 100vh;
        width: 100%;
        font-family: 'Patrick Hand', cursive, sans-serif;
        padding: 40px 0;
    }

    .login-center {
        display: flex;
        justify-content: center;
        align-items: center;
        width: 100%;
    }

    .register-card {
        width: 90%;
        max-width: 600px;
        background: rgba(255, 255, 255, 0.95);
        border: 3px solid #000;
        border-radius: 15px;
        box-shadow: 6px 6px 0 0 #000;
        padding: 30px;
        margin: 20px;
    }

    .ernte-parameter-card {
        max-width: 900px;
    }

    .logo-container {
        text-align: center;
        margin-bottom: 20px;
    }

    .app-logo {
        width: 80px;
    }

    .elitegaertner-title {
        font-family: 'Playfair Display', serif;
        font-weight: 600;
        font-size: 2.5rem;
        color: #000;
        margin-top: 8px;
        line-height: 1;
    }

    .register-title {
        font-weight: bold;
        font-size: 1.8rem;
        color: #000;
        margin-bottom: 20px;
        text-align: center;
        border-bottom: 2px solid #000;
        padding-bottom: 10px;
    }

    .tag-headline {
        font-weight: bold;
        font-size: 1.4rem;
        color: #000;
        margin-top: 20px;
        margin-bottom: 10px;
    }

    .form-layout {
        display: flex;
        gap: 30px;
        margin-bottom: 20px;
        align-items: center; /* Vertikale Zentrierung der Bild-Spalten */
    }

    .left-column {
        width: 250px;
        max-width: 40%;
        display: flex;
        flex-direction: column;
    }

    .right-column {
        flex: 1;
    }
    
    .ernte-image-container {
        border: 3px solid #000;
        box-shadow: 4px 4px 0 0 #000;
        background-color: #eee;
        border-radius: 8px;
        overflow: hidden;
        margin-bottom: 10px;
        min-height: 250px;
    }

    .image-placeholder {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        height: 100%;
        min-height: 250px;
        color: #666;
        font-size: 1.1rem;
    }

    .ernte-image {
        width: 100%;
        height: 100%;
        max-height: 400px;
        object-fit: cover;
        display: block;
    }

    .upload-button {
        text-align: center;
        padding: 8px 15px;
        font-size: 1.2rem;
    }

    .secondary-button {
        background-color: #ccc;
        color: #000;
    }

    .secondary-button:active {
        background-color: #bbb;
    }
    
    .tag-grid {
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
        margin-bottom: 20px;
    }

    .app-tag {
        display: flex;
        align-items: center;
        gap: 8px;
        padding: 6px 12px;

        border: 3px solid #000;
        border-radius: 8px;
        box-shadow: 2px 2px 0 0 #000;

        cursor: pointer;
        background-color: #f0f0f0;
        font-size: 1.1rem;
        font-weight: bold;
        transition: transform 0.1s, box-shadow 0.1s;
    }

    .app-tag:hover {
        transform: translate(1px, 1px);
        box-shadow: 1px 1px 0 0 #000;
    }

    .tag.selected {
        background-color: #8B45B3;
        color: white;
        border-color: #000;
        box-shadow: 4px 4px 0 0 #000;
        transition: transform 0.1s, box-shadow 0.1s;
    }

    .tag.selected:active {
        transform: translate(4px, 4px);
        box-shadow: 0 0 0 0 #000;
    }

    .tag.selected:hover {
        transform: none;
        box-shadow: 4px 4px 0 0 #000;
    }
    
    .input-group {
        margin-bottom: 15px;
        display: flex;
        flex-direction: column;
    }

    .input-label {
        font-weight: bold;
        margin-bottom: 5px;
        color: #000;
        font-size: 1.1rem;
    }

    .app-input {
        padding: 10px;
        border: 3px solid #000;
        outline: none;
        font-size: 1.1rem;
        box-shadow: 2px 2px 0 0 #000;
        font-family: 'Patrick Hand', cursive, sans-serif;
    }

    .parameter-row {
        display: flex;
        gap: 20px;
        margin-top: 20px;
    }

    .parameter-row .input-group {
        flex: 1;
        margin-bottom: 0;
    }

    textarea.app-input {
        height: 100px;
        resize: vertical;
    }

    .char-counter {
        text-align: right;
        font-size: 0.9rem;
        color: #666;
        margin-top: 5px;
    }

    /* Buttons */
    .app-button {
        width: 100%;
        padding: 12px;
        font-size: 1.5rem;
        font-weight: bold;
        text-align: center;
        border: 3px solid #000;
        border-radius: 8px;
        cursor: pointer;
        margin-top: 20px;

        box-shadow: 4px 4px 0 0 #000;
        transition: transform 0.1s, box-shadow 0.1s, background-color 0.2s;

        display: inline-block;
    }

    .primary-button {
        background-color: #8B45B3;
        color: white;
    }

    .app-button:active {
        transform: translate(4px, 4px);
        box-shadow: 0 0 0 0 #000;
    }

    .app-button:disabled {
        background-color: #ccc;
        color: #999;
        border-color: #999;
        cursor: not-allowed;
        transform: none;
        box-shadow: 2px 2px 0 0 #999;
    }

    .disabled-hint {
        text-align: center;
        color: #999;
        font-size: 1rem;
        margin-top: 10px;
        margin-bottom: 0;
        font-style: italic;
    }
</style>