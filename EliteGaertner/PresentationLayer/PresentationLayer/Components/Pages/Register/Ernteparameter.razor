@page "/registrierung/ernte-parameter"

<div class="ernte-parameter-container">
    <!-- Titel & Logo -->
    <div class="text-center mb-4">
        <img src="pictures/EliteGärtner-logo.png" alt="EliteGärtner Logo" style="width:50px;"/>
        <h2 class="elitegaertner-title mt-2" style="font-weight: 600; font-size: 1.5rem">EliteGärtner.</h2>
    </div>
    <div class="form-layout">

        <div class="left-column">
            <div class="ernte-image-container">
                <img src="@HochgeladenesBild" alt="Erntebild" class="ernte-image" />
            </div>
                
            <button class="publish-button-under-image" @onclick="Veröffentlichen" 
                    style="border-radius: 0 0 20px 20px; margin-top: -10px;">
                Ernte veröffentlichen
            </button>
        </div>

        <div class="right-column">
            <h3 style="margin-top: 0;">Gemüse</h3>
            <div class="tag-grid">
                @foreach (var tag in GemueseTags)
                {
                    <div class="tag @(SelectedTags.Contains(tag.Name) ? "selected" : "")"
                         @onclick="() => ToggleTag(tag.Name)">
                        <span class="emoji">@tag.Emoji</span> @tag.Name
                    </div>
                }
            </div>

            <h3>Obst</h3>
            <div class="tag-grid">
                @foreach (var tag in ObstTags)
                {
                    <div class="tag @(SelectedTags.Contains(tag.Name) ? "selected" : "")"
                         @onclick="() => ToggleTag(tag.Name)">
                        <span class="emoji">@tag.Emoji</span> @tag.Name
                    </div>
                }
            </div>
        </div>
    </div>
    <div class="parameter-row">
        <div class="input-group">
            <label>Gewicht (in g)</label>
            <input type="number" @bind="Gewicht" placeholder="z.B. 300" />
        </div>

        <div class="input-group">
            <label>Größe (in cm)</label>
            <input type="number" @bind="Groesse" placeholder="z.B. 25" />
        </div>
    </div>
    <h3 style="margin-top: 10px;">Kommentar</h3>
    <div class="input-group">
        <textarea maxlength="150" @bind="Kommentar"
                              placeholder="Beschreibe kurz deine Ernte..."></textarea>
        <div class="char-counter">@Kommentar.Length / 150</div>
    </div>
    </div>


    @code {
        public string HochgeladenesBild { get; set; } =
            "pictures/Testbilder/Gurken_01.jpg"; 

        public class TagItem(string name, string emoji)
        {
            public string Name { get; set; } = name;
            public string Emoji { get; set; } = emoji;
        }
        
        List<TagItem> GemueseTags = new()
        {
            new("Auberginen", "🍆"),
            new("Gurken", "🥒"),
            new("Tomaten", "🍅"),
            new("Kürbisse", "🎃"),
            new("Paprika", "🌶️"),
            new("Zucchini", "🥒"),
            new("Kartoffeln", "🥔"),
            new("Karotten", "🥕"),
            new("Salate", "🥬"),
            new("Zwiebeln", "🧅")
        };

        List<TagItem> ObstTags = new()
        {
            new("Melonen", "🍉"),
            new("Äpfel", "🍎"),
            new("Birnen", "🍐"),
            new("Pflaumen", "🍑"),
            new("Kirschen", "🍒"),
            new("Erdbeeren", "🍓"),
            new("Trauben", "🍇")
        };

        List<string> SelectedTags = new(); 

        int? Groesse { get; set; }
        int? Gewicht { get; set; }
        string Kommentar { get; set; } = "";

        void ToggleTag(string tag)
        {
            if (SelectedTags.Contains(tag))
                SelectedTags.Remove(tag);
            else
                SelectedTags.Add(tag);
        }

        void Veröffentlichen()
        {
            Console.WriteLine("Ausgewählte Tags: " + string.Join(", ", SelectedTags));
            Console.WriteLine($"Größe: {Groesse}, Gewicht: {Gewicht}");
            Console.WriteLine($"Kommentar: {Kommentar}");

            NavigationManager.NavigateTo("/main");
        }

        [Inject] NavigationManager NavigationManager { get; set; }
    }